NODE_ENV=development
PORT=5000

MONGODB_URI=mongodb://127.0.0.1:27017/tdp_party

JWT_ACCESS_SECRET=change_me_access_secret
JWT_REFRESH_SECRET=change_me_refresh_secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

CORS_ORIGINS=http://localhost:5173

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=tdp-party

SEED_ADMIN_EMAIL=admin@tdp.local
SEED_ADMIN_PASSWORD=ChangeMe123!
SEED_ADMIN_NAME=Super Admin

npm install firebase

// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyAwithJ3_8TxjV_H9do2lUbI-Q8W3fSLaI",
  authDomain: "tdp2-4d674.firebaseapp.com",
  projectId: "tdp2-4d674",
  storageBucket: "tdp2-4d674.firebasestorage.app",
  messagingSenderId: "386414628105",
  appId: "1:386414628105:web:2a8547550f4d24820157d8",
  measurementId: "G-L0ZPR0NLMG"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);




in voice calls or video calls after call receivers must get instantly like no need to on chat page or active messeging
after call receivers all actions hide and show the caller screen like in watsapp popup notification type coming,

"I need to add Firebase Cloud Messaging to my Node.js backend.

Install firebase-admin.

Create a new utility file notificationService.js to initialize Firebase using a service account JSON.

Add a function sendPushNotification(userToken, title, body) that I can call from my existing routes.

Create a new POST route /api/save-token that takes a userId and fcmToken and saves/updates it in my MongoDB User collection. Do not change my existing schema; just add a new field called fcmToken if it doesn't exist."


"I need to set up my React frontend to receive an FCM token from a native Android WebView.

Create a global function window.setFcmToken(token) that saves the token to localStorage.

When a user logs in, check if a token exists in localStorage. If it does, call my new /api/save-token backend route to link the token to that user.

Ensure the code is wrapped in a check to see if it's running inside an Android WebView."
